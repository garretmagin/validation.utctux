{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Aspire AppHost",
      "type": "dotnet",
      "request": "launch",
      "projectPath": "${workspaceFolder}/src/utctux.AppHost/utctux.AppHost.csproj",
      // "command": "aspire run",
      // "cwd": "${workspaceFolder}/src/utctux.AppHost",
      "preLaunchTask": "build"
    }
  ]
}
